# LAM-A2E — 검증된 venv 의 pip freeze 에서 requirements-lam.txt 에 빠져 있던 나머지.
#
# P3 첫 콜드 요청이 LAM_FAILED 로 실패한 원인이 여기 있었다: utils/config.py 가
# yapf 를 import 하는데 핀 목록에 없었다. P2 의 핀은 검증된 venv 의 *일부*였다.
# 이 파일은 그 venv 의 freeze 전체(runpod/lam-venv-freeze.txt)와 기존 핀의 차집합이다.
#
# 별도 파일인 이유: requirements-lam.txt 를 바꾸면 4.86 GB torch 레이어의 캐시가
# 깨져 재빌드·재푸시(~70 분)가 든다. 여기 있는 것은 순수 파이썬 패키지 수십 MB 다.
# P3 이후 두 파일을 하나로 합치고 이미지를 다시 굽는다.
audioread==3.1.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cloudpickle==3.1.2
colorama==0.4.6
decorator==5.3.1
filelock==3.32.5
fsspec==2026.7.0
huggingface_hub==0.36.2
idna==3.19
importlib_metadata==9.0.1
Jinja2==3.1.6
joblib==1.6.0
lazy-loader==0.5
llvmlite==0.49.0
MarkupSafe==3.0.3
mpmath==1.3.0
msgpack==1.2.2
networkx==3.4.2
packaging==26.3
platformdirs==4.11.7
pooch==1.9.0
pycparser==3.0
PyYAML==6.0.3
regex==2026.9.3
requests==2.34.2
safetensors==0.8.0
scikit-learn==1.7.2
soxr==1.1.0
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.15.2
tomli==2.4.1
tqdm==4.70.0
typing_extensions==4.16.0
urllib3==2.7.0
yapf==0.40.1
zipp==4.1.0
